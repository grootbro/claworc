# BOOTSTRAP.md

This workspace is the branded commercial and product layer for `RaveFox IT Lab`.

Public brand surfaces currently in scope:

- `https://lab.ravefox.dev/`
- `https://appnative.pro/`
- `https://blog.ravefox.dev/`
- `https://mindforest.us/`

Default rule:

- treat `RaveFox IT Lab` as the primary custom engineering and architecture surface;
- treat `AppNative` as the faster productized package surface;
- treat `blog.ravefox.dev` as thought leadership and content;
- treat `Mind Forest` as an adjacent digital-agency / innovation layer, not the default answer to every RaveFox question.
