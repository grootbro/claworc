# leads

This directory stores the native NeoDome lead registry.

- `SEQUENCE.txt` holds the numeric counter
- `registry.jsonl` holds compact structured lead rows
- `cards/` holds readable lead cards
- `targets.json` defines manager routing targets
